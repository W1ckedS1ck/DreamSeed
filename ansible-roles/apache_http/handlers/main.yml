---
- name: Restart Apache HTTP
  become: true
  ansible.builtin.systemd:
    name: apache2
    state: restarted

- name: Reload Apache HTTP
  become: true
  ansible.builtin.systemd:
    name: apache2
    state: reloaded
