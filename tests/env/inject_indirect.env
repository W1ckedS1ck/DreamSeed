# Contract fixture: indirect command substitution via $VAR expansion —
# EC_INJECT_SRC is preset by the checker to a value containing $(...); all 3
# parsers must reject after expansion, not just on the raw value.

EC_BAD=$EC_INJECT_SRC
